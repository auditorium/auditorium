<% 
count = current_user.unread_notifications.count
count += unread_membership_requests.count if unread_membership_requests
%> 
<%= link_to count, notifications_path, :class=>"notification-button #{'red' if count > 0}", title: "#{count} unread notifications" %>

