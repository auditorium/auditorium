<%= link_to current_user.unread_notifications.count, notifications_path, :class=>"notification-button #{'red' if current_user.unread_notifications.count > 0}", title: "#{current_user.unread_notifications.count} unread notifications" %>

