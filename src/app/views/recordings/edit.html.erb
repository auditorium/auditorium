<div class="container span8 center">
<h2><%= t('recordings.headline.edit') %></h2>
<p class="hint">update the recording for <%= link_to @course.name_with_term, @course %></p>


<%= render 'recordings/form' %>
</div>