if($('.manage-user-table').length > 0) { $('.manage-user-table').html('<%= escape_javascript(render "course/manage-users-table") %>'); }
$('#saved-changes').hide();
$('input#save-changes').removeAttr("disabled");