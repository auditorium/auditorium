<article>
  <header>
    <h2><%= t('recordings.edit.title') %></h2>    
  </header>

  <div  id="recording">
    <%= form_for(@recording) do |f| %>
      <ul class="no-style">
        <li class="input-fields">
          <%= f.label :subject %>
          <%= f.text_field :subject %>
        </li>

        <li class="input-fields">
          <%= f.label :url %>
          <%= f.url_field :url %>
        </li>
        
        <li class="input-fields">
          <%= f.label :content %>
          <%= f.text_area :content %>
        </li>
        
        <li class="input-fields">
          <%= f.label :tag_tokens %>
          <%= f.text_field :tag_tokens, data: {load: @recording.tags} %>
          <p class="tags_hint hint"><%= t('groups.group_types.general.tags_hint')%></p>
        </li>
        
        <li class="input-fields">
          <%= f.submit t('groups.new.submit'), id: 'submit', class: 'small button' %>
        </li>
      </ul>
    <% end %>
  </div>
</article>