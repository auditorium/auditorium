<!doctype html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
    <%= stylesheet_link_tag 'emails' %>
  </head>

  <body>
    <div id="email-header">
      <h2><%= t('emails.greetings.hello', user: @resource) %></h2>
    </div>

    <div id="email-content">
      <p><%= t('emails.content.subjects.change_password') %></p>
      <p><%= link_to t('emails.content.goto.change_password'), edit_password_url(@resource, :reset_password_token => @token, locale: I18n.locale), class: 'button alert' %></p>
    </div>

    <p><%= t('emails.footer.not_requested') %></p>
    <p><%= t('emails.footer.password_will_not_change')%></p>


    <p class="email-greetings">
      <%= t('emails.greetings.regards') %>
    </p>
  </body>
</html>


