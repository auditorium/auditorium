Hi, <%= @user.full_name %>
=======================================================

<% @post.post_type.eql? 'info' ? type = 'announcement' : type = @post.post_type %>

There is a new <%= 'private' if @post.origin.is_private? %> <%= type %> in <%= @post.course.name_with_term %> written by <%= @post.author.full_name %>

<% if @post.post_type.eql? 'question' or @post.post_type.eql? 'info' %>
Subject:
<%= @post.subject %>
---
<% end %>
      
Content:
<%= @post.body %>
---

<% if @post.origin.post_type.eql? 'question' %>
<%= "View, answer or comment on #{@post.origin.author}'s question <#{post_url(@post.origin)}>" %>
<% else %>
<%= "View or comment on #{@post.origin.author}'s announcement <#{post_url(@post.origin)}>" %>
<% end %>

You receive this email, because you subscribed to <%= @post.course.name_with_term %> <%= course_url(@post.course) %> Change what emails auditorium sends you <%= edit_user_registration_url %>